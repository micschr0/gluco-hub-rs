version: "3"

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list

  build:
    desc: Build (debug)
    cmd: cargo build

  build-release:
    desc: Build (release)
    cmd: cargo build --release

  build-all:
    desc: Build with all features (llu + nightscout + mqtt)
    cmd: cargo build --features "source-llu sink-nightscout sink-mqtt"

  test:
    desc: Run tests
    cmd: cargo test

  test-all:
    desc: Run tests with all features
    cmd: cargo test --all-features

  test-verbose:
    desc: Run tests with output
    cmd: cargo test --all-features -- --nocapture

  lint:
    desc: Run clippy (warnings as errors)
    cmd: cargo clippy --all-targets -- -D warnings

  fmt:
    desc: Format code
    cmd: cargo fmt --all

  fmt-check:
    desc: Check formatting without applying
    cmd: cargo fmt --all -- --check

  audit:
    desc: Check dependencies (cargo-deny)
    cmd: cargo deny check

  check:
    desc: fmt + lint + test
    cmds:
      - task: fmt
      - task: lint
      - task: test-all

  run:
    desc: Run with default config
    cmd: cargo run -- run

  run-config:
    desc: Run with config.toml
    cmd: cargo run -- run -c config.toml

  docker:
    desc: Build container image (gluco-hub:dev)
    cmd: docker build -t gluco-hub:dev .

  release:dry:
    desc: Preview the next minor release without making changes
    cmd: cargo release minor

  release:minor:
    desc: Cut a minor release (commits, tags, pushes)
    cmd: cargo release minor --execute
