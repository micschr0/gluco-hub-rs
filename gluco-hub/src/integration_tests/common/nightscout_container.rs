// SPDX-License-Identifier: AGPL-3.0-or-later

//! Nightscout + MongoDB testcontainer harness — populated in Phase B.
//!
//! Phase A only ships the MQTT testcontainer; this stub keeps the
//! module tree intact so `cfg(feature = "sink-nightscout")` builds
//! continue to compile while Phase B is in flight.
